/* dts binding: seeed,grove-nfc */
description: Seeed Grove NFC module (I2C register-mapped NFC front-end)

compatible: "seeed,grove-nfc"

include: i2c-device.yaml

properties:
  label:
    type: string
    required: false
